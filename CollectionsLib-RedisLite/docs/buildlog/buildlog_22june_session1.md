Date: June 22
Duration: 120 minutes

Goal: create the dynamicArray and implement all its functions and start linkedlist at the end of the day

Problem Encountered:

When i was developing a generic dynamic array template i faced difficulties on supporting different data types. it requires Proper construction, destruction, copying of objects required careful use of templates and memory management to ensure correctness for both primitive and user-defined types.

What I Tried:

I Converted the dynamic array into a template class to support both primitive and user-defined data types. and also used copy constructor and assignment operator overloading 
to prevent double deletion and deep copy instead of shallow copy.

Outcome:

created a generic dynamic array template  and start implementing its functions.